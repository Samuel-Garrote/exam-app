generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id   Int    @id @default(autoincrement())
  name String
}

model Result {
  id             Int   @id @default(autoincrement())
  correctAnswers Int
  totalQuestions Int
  score          Float
}
